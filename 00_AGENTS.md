# AGENTS.md

## 0. Purpose

このファイルは、本リポジトリでCodexが開発作業を行う際の最上位の実装ルールを定義する。

本プロジェクトの初期目標は、パーティーゲームアプリを短期間で実際にスマートフォン上で遊べる状態へ完成させること。

Codexは、仕様を勝手に拡張するのではなく、既存ドキュメントに従い、小さく・安定して・検証可能な形で実装すること。

------------------------------------------------------------------------

# 1. Source of Truth

実装前に必ず以下を確認すること。

1.  `docs/01_REQUIREMENTS.md`
2.  `docs/02_ARCHITECTURE.md`
3.  `docs/03_UI_FLOW.md`
4.  `AGENTS.md`

役割：

-   `01_REQUIREMENTS.md` = 何を作るか
-   `02_ARCHITECTURE.md` = どう作るか
-   `03_UI_FLOW.md` = ユーザーがどう操作するか
-   `AGENTS.md` = Codexがどう開発するか

仕様判断では上記ドキュメントを優先する。

実装とドキュメントに矛盾がある場合、勝手に仕様を作らず、既存仕様を基準に修正する。

------------------------------------------------------------------------

# 2. MVP Goal

初期MVPの目的は、

**「実際のBBQ・家族・友人との集まりで、スマートフォン1台を置いたまま2つのゲームを最後まで遊べること」**

である。

初期ゲーム：

1.  爆弾ゲーム
2.  究極の2択

見た目の豪華さや高度な技術より、実際に遊べることを優先する。

------------------------------------------------------------------------

# 3. Development Priority

優先順位：

1.  正常に起動する
2.  スマートフォンで操作できる
3.  ゲームを最初から最後まで遊べる
4.  ゲームロジックが正しい
5.  音声・タイマーが安定する
6.  データが正しく保持される
7.  UIを整える
8.  演出を追加する

装飾や高度な演出のために、ゲーム成立を遅らせてはならない。

------------------------------------------------------------------------

# 4. Technology Rules

基本技術：

-   Next.js
-   React
-   TypeScript
-   App Router
-   Vercel
-   localStorage

初期MVPでは原則として以下を追加しない。

-   外部DB
-   独自バックエンド
-   ログイン
-   ユーザー認証
-   WebSocket
-   有料API
-   AI API
-   音声認識API
-   外部TTS API
-   不要な状態管理ライブラリ
-   不要なUIフレームワーク
-   不要な大規模依存パッケージ

標準のWeb APIとNext.js/Reactで十分な場合は、それを優先する。

------------------------------------------------------------------------

# 5. Do Not Overengineer

初期MVPでは過剰設計を禁止する。

人間が簡単に判断できることを、無理にアプリへ入力・自動判定させない。

例：

-   早口言葉の成功判定 → 人間
-   爆発時に誰がボールを持っていたか → 人間
-   究極の2択の投票 → 人間
-   多数派・少数派判定 → 人間
-   最終敗者判定 → 人間

アプリは司会・演出・ランダム処理・お題表示を担当する。

------------------------------------------------------------------------

# 6. Architecture Principles

ゲームごとの処理を可能な限り独立させる。

共通化する候補：

-   localStorage
-   ランダム抽選
-   音声再生
-   参加者処理
-   共通UI
-   確認ダイアログ

ゲーム固有ロジックは、それぞれのゲーム側へ置く。

新ゲーム追加時に既存ゲームを大きく変更する構造にしない。

------------------------------------------------------------------------

# 7. Configuration Rules

実地テストで変更する可能性が高い値を、コンポーネント内へ散在させない。

一元管理対象：

-   爆発時間範囲
-   特殊指令発生間隔
-   特殊指令抽選ウェイト
-   特殊指令保護時間
-   フェイク効果音発生間隔
-   その他ゲーム調整値

`lib/game-config.ts` 等にまとめる。

実際に遊んだ後、数値だけ変更して調整できる状態を維持する。

------------------------------------------------------------------------

# 8. Bomb Game: Critical Rule

爆弾ゲームで最重要の技術要件：

**ゲームSTART時に爆発予定時刻を1回だけ決定し、その後は絶対に変更しない。**

特殊指令、フェイク効果音、音声再生、UI演出等を理由に爆発を遅延させてはならない。

爆発予定時刻になったら、他のイベントより爆発を優先する。

------------------------------------------------------------------------

# 9. Bomb Timers

爆弾ゲームでは少なくとも以下を独立管理する。

-   explosion timer
-   special-command timer
-   fake-sound timer

概念：

``` text
START
│
├─ explosion timer
├─ special-command timer
└─ fake-sound timer
```

ゲーム終了・画面遷移・コンポーネント破棄時には、すべての不要なタイマーを確実に解除する。

ゲーム終了後にイベントや音が発生してはならない。

------------------------------------------------------------------------

# 10. Bomb Audio

最低限必要な音：

-   ticking
-   explosion
-   special commands
-   fake sounds

音源は原則 `public/sounds/` 配下で管理する。

固定司会音声は原則として、

**1イベント = 1音声ファイル**

とする。

爆発時は不要な音声を停止し、爆発音を優先する。

音源読み込み失敗によってゲームロジック自体をクラッシュさせない。

------------------------------------------------------------------------

# 11. Mobile Audio

スマートフォンブラウザの自動再生制限を考慮する。

ゲームSTARTなど、明確なユーザー操作を起点としてAudioを利用可能な状態にする。

PCブラウザだけで動作確認して完了としない。

音声を使用する機能はスマートフォン実機確認を前提とする。

------------------------------------------------------------------------

# 12. Bomb Command Rules

初期特殊指令：

-   リバース
-   1人飛ばし
-   2人飛ばし
-   お前は2回じゃ！
-   お前は3回じゃ！

HOLD系の指令は実装しない。

抽選ウェイト：

-   reverse: 25
-   skipOne: 25
-   skipTwo: 15
-   repeatTwo: 20
-   repeatThree: 15

OFFの指令は抽選候補から除外する。

残り候補で重み付き抽選する。

同じ指令を原則2回連続で出さない。

ただし有効な指令が1種類しかない場合は再発を許可する。

------------------------------------------------------------------------

# 13. Bomb Event Density

初期値：

``` ts
low:     18–30 sec
normal:  12–22 sec
high:     8–15 sec
brutal:   6–11 sec
```

数値は実地テストで変更する前提。

特殊指令数そのものは固定しない。

短いゲームでは特殊指令が0回でも正常。

------------------------------------------------------------------------

# 14. Bomb Explosion Ranges

初期値：

``` ts
short:  10–40 sec
normal: 20–70 sec
long:   50–120 sec
chaos:  10–120 sec
```

残り秒数・爆発予定時刻・プログレスバーをゲーム画面へ表示しない。

------------------------------------------------------------------------

# 15. Fake Sounds

フェイク効果音は特殊指令と別系統。

フェイク音にはゲーム上の効果を持たせない。

初期MVPでは、

-   ON
-   OFF

のみでよい。

個別音源選択UIは不要。

特殊指令が聞こえなくなるような音声衝突は避ける。

ただし爆発時刻は音声衝突回避のために遅らせない。

------------------------------------------------------------------------

# 16. Tongue Twisters

型：

``` ts
type TongueTwister = {
  id: string;
  text: string;
  difficulty: "easy" | "normal" | "hard";
};
```

1ラウンド中は同じ早口言葉を使用する。

参加者ごとに変更しない。

成功判定はアプリで行わない。

使用済みIDをlocalStorageへ保存する。

------------------------------------------------------------------------

# 17. Ultimate Choice

型：

``` ts
type UltimateChoice = {
  id: string;
  optionA: string;
  optionB: string;
  categories: string[];
  level: "family" | "adult" | "hard";
};
```

レベル：

-   FAMILY
-   ADULT
-   HARD

複数選択可能。

通常戦と決定戦は同じ使用済み履歴を共有する。

アプリは投票数・多数派・少数派・勝敗を自動判定しない。

自動で次の問題へ進まない。

------------------------------------------------------------------------

# 18. Prompt History

localStorageに最低限以下を保存する。

-   `players`
-   `usedTongueTwisters`
-   `usedUltimateChoices`

画面遷移やゲーム終了では出題履歴を削除しない。

HOMEの「出題履歴をリセット」が実行された場合のみ、使用済み早口言葉・究極の2択履歴を削除する。

参加者情報は削除しない。

------------------------------------------------------------------------

# 19. Participant Rules

参加者登録は任意。

登録なしでもゲームを開始できる。

参加者データ：

``` ts
type Player = {
  id: string;
  name: string;
};
```

HOMEの参加者名簿と、ゲームごとの参加ON/OFF状態を混同しない。

爆弾ゲームでは、選択された登録参加者が複数いる場合に開始者ルーレットを利用できる。

登録者がいない場合は、人間が開始者を決める。

------------------------------------------------------------------------

# 20. localStorage Safety

localStorage読み込み時は以下を考慮する。

-   データなし
-   不正JSON
-   古い形式
-   空配列

異常データによってアプリ全体をクラッシュさせない。

合理的な初期値へフォールバックする。

------------------------------------------------------------------------

# 21. Prompt Exhaustion

条件に合う未使用問題が0件になった場合、勝手に履歴を削除して再利用しない。

ユーザーへ、

「この条件のお題を全部使いました」

等を表示する。

条件変更またはHOMEの履歴リセットへ誘導する。

------------------------------------------------------------------------

# 22. UI Rules

スマートフォン縦画面最優先。

主要操作：

-   大きなボタン
-   十分なタップ領域
-   高い可読性
-   明確な階層
-   少ない入力

ゲーム中の重要情報は大きく表示する。

爆弾ゲームでは早口言葉と特殊指令を最優先。

究極の2択ではA/Bを最優先。

------------------------------------------------------------------------

# 23. Navigation Safety

爆弾ゲームPLAYING中にHOME等へ移動する場合は、必要に応じて確認を入れる。

終了時は必ず、

-   explosion timer
-   command timer
-   fake timer
-   ticking
-   その他再生中Audio

を停止・破棄する。

ブラウザの戻る操作等でも可能な限りクリーンアップする。

------------------------------------------------------------------------

# 24. PWA Scope

初期MVPでは本格的なPWA・完全オフライン対応を完成条件にしない。

優先：

1.  Vercelで公開
2.  スマートフォンで開ける
3.  実際にゲームが成立する
4.  必要ならホーム画面から起動しやすくする

Service
Worker、完全オフラインキャッシュ、高度な更新管理は後回しにできる。

------------------------------------------------------------------------

# 25. TTS Scope

初期MVPでは究極の2択や早口言葉の自動TTS読み上げを必須としない。

人間が画面を読めばゲームが成立する。

将来的なTTS追加を妨げないデータ構造にしておけばよい。

------------------------------------------------------------------------

# 26. Implementation Order

初回実装は以下の順番を基本とする。

## Phase 1

プロジェクト土台。

-   Next.js
-   TypeScript
-   HOME
-   共通UI
-   基本レスポンシブ

## Phase 2

爆弾ゲームを最初から最後まで完成。

-   遊び方
-   設定
-   早口言葉
-   開始者
-   START
-   ticking
-   特殊指令
-   フェイク音
-   explosion
-   終了

## Phase 3

究極の2択を完成。

-   遊び方
-   設定
-   通常問題
-   次の問題
-   決定戦
-   再決定戦

## Phase 4

共通機能を完成。

-   参加者管理
-   参加ON/OFF
-   localStorage
-   履歴リセット

## Phase 5

Vercelへ公開し、スマートフォン実機テスト。

------------------------------------------------------------------------

# 27. Change Discipline

既存仕様にない機能を「便利そうだから」という理由だけで追加しない。

特に以下を勝手に追加しない。

-   アカウント
-   DB
-   管理画面
-   投票システム
-   勝敗自動判定
-   AI
-   音声認識
-   複雑なアニメーション
-   オンライン対戦
-   大規模UIライブラリ

追加が必要だと判断した場合は、実装せず理由を報告する。

------------------------------------------------------------------------

# 28. Refactoring Discipline

期限優先だが、動けば何でもよいという意味ではない。

以下は避ける。

-   巨大な単一コンポーネント
-   同じロジックの大量コピー
-   タイマー処理の散在
-   調整値のハードコード乱立
-   型を無視した実装
-   不要な `any`
-   未使用コードの放置

ただし、将来使うかもしれないという理由だけで抽象化を増やさない。

------------------------------------------------------------------------

# 29. Testing Before Completion

実装完了を宣言する前に、可能な範囲で以下を確認する。

## Build

-   TypeScriptエラーがない
-   buildが成功する
-   明白なlintエラーがない

## Navigation

-   HOMEから各画面へ移動できる
-   HOMEへ戻れる
-   戻った後にゲームタイマーが残らない

## Bomb

-   爆発時刻が固定される
-   tickingが開始・停止する
-   特殊指令が動く
-   OFF設定が反映される
-   フェイク音ON/OFFが反映される
-   爆発後にイベントが残らない

## Ultimate Choice

-   レベル選択が反映される
-   問題が出る
-   使用済み問題を避ける
-   決定戦へ移行できる
-   再決定戦できる

## Storage

-   参加者が保持される
-   出題履歴が保持される
-   履歴リセットで参加者まで消えない

------------------------------------------------------------------------

# 30. Real Device Validation

このアプリはPC上での動作だけでは完成とみなさない。

特に以下はスマートフォンで確認する。

-   音声再生
-   音量
-   ticking
-   爆発音
-   特殊指令
-   ボタンサイズ
-   スクロール
-   画面スリープの影響
-   実際にテーブルへ置いた状態で文字が読めるか

Codexが実機確認できない場合は、ユーザーが確認すべき項目を明確に報告する。

------------------------------------------------------------------------

# 31. When Requirements Are Ambiguous

既存ドキュメントから合理的に判断できる軽微な実装詳細は、MVPを優先してシンプルな方法を選ぶ。

ゲームルール・ユーザー体験・データ消去・爆発タイミング等に影響する重要な不明点は、勝手に新仕様を作らない。

その場合は、

-   何が不明か
-   選択肢
-   MVPとして推奨する最小案

を簡潔に報告する。

------------------------------------------------------------------------

# 32. Completion Report Format

作業終了時は、長い説明ではなく以下を簡潔に報告する。

## 実装したもの

変更・追加した主要機能。

## 主な変更ファイル

主要ファイルのみ。

## 確認したこと

build、テスト、主要フロー等。

## ユーザー実機確認が必要なこと

スマートフォン音声・操作など。

## 残っていること

未実装・既知の問題がある場合のみ記載。

------------------------------------------------------------------------

# 33. Definition of Done

機能は「コードを書いた」だけでは完了ではない。

以下を満たして完了とする。

1.  要件に沿っている
2.  主要フローが最後まで通る
3.  build可能
4.  明白なエラーがない
5.  不要なタイマー・Audioが残らない
6.  スマートフォン利用を阻害する設計になっていない
7.  変更内容を簡潔に説明できる

最終判断基準は、

**「実際の集まりで、説明に困らず、スマートフォンを置いたまま最後まで遊べるか」**

である。
